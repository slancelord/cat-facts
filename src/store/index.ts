import Vue from 'vue'
import Vuex from 'vuex'
import catFact from './catFact'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    catFact
  }
})
